# cercoachapp dependencies — same pinned set as pantryapp/choresapp, resolved on
# Python 3.14. All ship prebuilt wheels for cp314, so a plain
# `pip install -r requirements.txt` needs no compiler.
#
# Install:  .venv/bin/pip install -r requirements.txt
fastapi==0.136.3
sqlmodel==0.0.38
uvicorn[standard]==0.48.0
jinja2==3.1.6
python-multipart==0.0.29
